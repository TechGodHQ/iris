[package]
name = "iris-codegen"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Code generation for Iris — hydra-backed, keeps CLI, MCP, and HTTP surfaces in sync"

[lints]
workspace = true

[dependencies]
hydra-core = { git = "https://github.com/TechGodHQ/hydra.git", branch = "main" }
hydra-codegen = { git = "https://github.com/TechGodHQ/hydra.git", branch = "main" }
anyhow = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
tempfile = "3"
